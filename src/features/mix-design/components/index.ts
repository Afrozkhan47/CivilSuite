/**
 * Mix Design — Shared Components
 *
 * This directory contains shared React components specific to the
 * Concrete Mix Design feature. Components here should be reusable
 * across the various steps of the mix design workflow.
 *
 * Current app pages still use components in src/app/concrete-mix-design/components/.
 * As the workflow grows, shared components can be extracted here.
 */

export {};
