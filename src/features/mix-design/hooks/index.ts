/**
 * Mix Design — Custom Hooks
 *
 * React hooks that wrap the Mix Design Service for use in UI components.
 * Hooks must call the service layer — they must NOT call the calculation
 * engine or Supabase directly.
 *
 * Example future hooks:
 *   useMixDesignCalculation(input)  → triggers service, returns result + state
 *   useSavedProjects()              → fetches projects via service (localStorage / Supabase)
 *   useProjectValidation(input)     → runs validation and returns issues
 */

export {};
